# PostgreSQL connection string. Use the same URL from Vercel Postgres locally.
# In Vercel: Storage → your Postgres → .env / Connect tab.
# DATABASE_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
